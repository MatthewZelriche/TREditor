/// <summary>
/// Persistent identity mapping between a positive mesh material slot and a normalized texture
/// asset ID.
/// </summary>
public readonly record struct MaterialSlotMapping(int Slot, string AssetId);
